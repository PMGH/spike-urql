import { Country } from "types/shared.types";

export type CountriesPageProps = {
  staticCountries: Country[]
}
export type FeaturedCountryProps = {
  code: string;
}
export type StaticCountriesListProps = {
  countries: Country[]
}
